# 渲染领域参考文件

> 本文件定义渲染（render）阶段的规范。执行 `/recastory render` 或 `/recastory craft` 时加载。

---

## 状态：占位文件

本文件为初始化占位，内容待补充。渲染规范将涵盖：

- 输出分辨率（1920×1080）
- 编码参数（H.264, CRF 18-23）
- 字幕样式与同步要求（偏差 ≤0.5 秒）
- 录屏三种模式（手动/音频/自动）
- 文件大小控制（短视频 ≤500MB）
- 音视频时长对齐（偏差 ≤1 秒）
